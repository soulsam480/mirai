{
  "name": "@mirai/api",
  "version": "0.0.0",
  "description": "",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "require": "./src/index.ts"
    }
  },
  "scripts": {
    "db-up": "docker-compose up -d",
    "db-seed": "prisma db seed",
    "db-migrate-dev": "yarn prisma migrate dev --schema=../prisma/schema.prisma",
    "db-nuke": "docker-compose down --volumes --remove-orphans",
    "dev": "ts-node-dev --respawn --transpile-only --clear --rs --watch src ./src/app.ts",
    "dx": "cross-env DATABASE_URL=postgresql://postgres:@localhost:5632/mirai run-s db-up db-migrate-dev db-seed dev",
    "ts-node": "ts-node -T --compiler-options \"{\\\"module\\\":\\\"commonjs\\\"}\"",
    "build-1": "prisma migrate deploy",
    "postinstall": "prisma generate --schema=../prisma/schema.prisma"
  },
  "prisma": {
    "seed": "yarn ts-node ../prisma/seed.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mirai/app": "workspace:*",
    "@trpc/server": "^9.20.3",
    "bcrypt": "^5.0.1",
    "dotenv": "^16.0.0",
    "fastify": "^3.0.0",
    "fastify-autoload": "^3.10.0",
    "fastify-cli": "^2.15.0",
    "fastify-plugin": "^3.0.0",
    "fastify-sensible": "^3.1.2",
    "prisma": "^3.11.0",
    "prisma-exclude": "^1.0.1",
    "superjson": "^1.8.1",
    "tsconfig-paths": "^3.14.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "ts-node-dev": "^1.1.8"
  }
}
